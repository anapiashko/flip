setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: java -jar /home/appuser/app/app.jar JAVA_OPTS='-Xmx384m -Xss512k -XX:+UseCompressedOops'
